# 2026-07-06: wt/tsumu MERGED to master and RETIRED (branch + worktree deleted).
# Tsumu now builds against the shared master checkout like MMX4/5/6 + Tomba. All
# of Tsumu's framework work (RAM 4x mirror, cdrom Stop 0x08 / MotorOn 0x07, the
# localization tier, the text-image-guard byte-compare + dirty_ram_text_bless
# fix) is on master, plus the HLE-boot-default flip. Regen game C against master
# before a release build (codegen unchanged since the merge, but regen out of
# caution). Prev pin: c586bad on the deleted wt/tsumu branch.
branch=master
sha=864ac40
